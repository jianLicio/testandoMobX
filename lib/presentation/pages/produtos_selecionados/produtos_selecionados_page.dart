import 'package:flutter/material.dart';

class ProdutoSelecionadosPage extends StatelessWidget {
  const ProdutoSelecionadosPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Produtos Selecionados'),
    );
  }
}
